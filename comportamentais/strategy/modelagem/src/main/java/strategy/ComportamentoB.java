package strategy;

/**
 * Interface que representa outro comportamento.
 * Cada comportamento é uma interface. Podem existir várias implementações
 * de um mesmo comportamento.
 * O total de comportamentos depende do problema que você estiver resolvendo.
 * Deve ter seu nome alterado para algo específico.
 * @author Manoel Campos da Silva Filho
 */
public interface ComportamentoB {
    /**
     * Método para execução do comportamento.
     * O tipo do retorno e possíveis parâmetros deve ser alterado
     * conforme as necessidades do problema a ser resolvido.
     * Deve ter seu nome alterado para algo específico.
     * @return
     */
    int executarComportamentoB();
}
